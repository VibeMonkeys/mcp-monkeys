server:
  port: 8095

spring:
  application:
    name: calendar-mcp-server
  ai:
    openai:
      api-key: ${OPENAI_API_KEY:dummy-key}
    mcp:
      server:
        enabled: true
        name: calendar-mcp-server
        version: 1.0.0
        type: SYNC
        description: "Calendar event management service"
        sse-message-endpoint: /mcp/messages
        capabilities:
          tool: true
          resource: false
          prompt: false
          completion: true

management:
  endpoints:
    web:
      exposure:
        include: health,info,mcp
  endpoint:
    health:
      show-details: always

logging:
  level:
    com.monkeys.calendar: DEBUG
    org.springframework.ai: INFO